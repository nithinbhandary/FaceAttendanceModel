function showLoadingScreen() {
    document.getElementById('loading-screen').style.display = 'flex';
}

    // // Get the alert message from Flask
    // var alertMessage = "{{ alert_message }}";

    // // Display an alert if the message is not empty
    // if (alertMessage) {
    // alert(alertMessage);
    // }